pipeline {
    agent {
        label 'dockerslave'
    }
    stages {
        stage('Test') {
            steps {
                sh '''
                pwd
                ipconfig
                '''
            }
        }
    }
}